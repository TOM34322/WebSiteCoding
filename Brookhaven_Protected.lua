-- Protected by CodeBlox Studio
-- Game: Brookhaven
-- Date: 27/03/2026 22:42:23

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))() local Players = game:GetService("Players") local RunService = game:GetService("RunService") local player = Players.LocalPlayer local isRunning = false local playerSpeed = 100 local noclipEnabled = false local autoJump = false local collisionConnection = nil local eggFolders = { "EggHunt_Easy", "EggHunt_Medium", "EggHunt_Hard", "EggHunt_Eggstreme" } local jumpStuckTimer = 0 local lastJumpCheckPos = Vector3.new(0, 0, 0) RunService.Heartbeat:Connect(function(dt) if not autoJump then return end if not isRunning then return end if not player.Character then return end local hrp = player.Character:FindFirstChild("HumanoidRootPart") local humanoid = player.Character:FindFirstChild("Humanoid") if not hrp or not humanoid then return end local moved = (hrp.Position - lastJumpCheckPos).Magnitude if moved < 0.3 then jumpStuckTimer = jumpStuckTimer + dt if jumpStuckTimer >= 0.3 then humanoid.Jump = true jumpStuckTimer = 0 end else jumpStuckTimer = 0 lastJumpCheckPos = hrp.Position end end) local function setSitMode(sit) if not player.Character then return end local humanoid = player.Character:FindFirstChild("Humanoid") if not humanoid then return end if sit then humanoid.Sit = true humanoid.AutoRotate = false else humanoid.Sit = false humanoid.AutoRotate = true end end local function getEggPart(egg) if egg:IsA("BasePart") then return egg end return egg:FindFirstChildWhichIsA("BasePart", true) or egg.PrimaryPart end local function isEggCollected(egg) if not egg or not egg.Parent then return true end return false end local function getAllEggs() local eggs = {} for _, folderName in ipairs(eggFolders) do local folder = workspace:FindFirstChild(folderName) if folder then for _, egg in ipairs(folder:GetChildren()) do local part = getEggPart(egg) if part then table.insert(eggs, { egg = egg, part = part, collected = isEggCollected(egg) }) end end end end return eggs end local function getUncollectedEggs() local allEggs = getAllEggs() local uncollected = {} for _, eggData in ipairs(allEggs) do if not eggData.collected then table.insert(uncollected, eggData.part) end end return uncollected end local function addClickDetectorsToEggs() local eggs = getAllEggs() for _, eggData in ipairs(eggs) do local part = eggData.part if part and not part:FindFirstChild("EggClickDetector") then local clickDetector = Instance.new("ClickDetector") clickDetector.Name = "EggClickDetector" clickDetector.Parent = part clickDetector.MaxActivationDistance = 15 clickDetector.MouseClick:Connect(function(clickingPlayer) if clickingPlayer == player then local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart") if hrp then local bp = Instance.new("BodyPosition") bp.Position = part.Position + Vector3.new(0, 2, 0) bp.MaxForce = Vector3.new(1e5, 1e5, 1e5) bp.P = 5000 bp.D = 500 bp.Parent = hrp task.wait(0.5) bp:Destroy() Rayfield:Notify({ Title = "🥚 Egg Collected", Content = eggData.egg.Name, Duration = 2 }) end end end) end end end task.spawn(function() while true do task.wait(5) addClickDetectorsToEggs() end end) local function moveToEgg(targetPart) if not targetPart or not player.Character then return end local hrp = player.Character:FindFirstChild("HumanoidRootPart") if not hrp then return end workspace.CurrentCamera.CameraType = Enum.CameraType.Follow lastJumpCheckPos = hrp.Position local goal = targetPart.Position + Vector3.new(0, 2, 0) local dist = (hrp.Position - goal).Magnitude local speed = math.max(playerSpeed, 20) local duration = math.clamp(dist / speed, 0.3, 8) local bv = Instance.new("BodyVelocity") bv.MaxForce = Vector3.new(1e5, 1e5, 1e5) bv.P = 1e4 bv.Velocity = Vector3.new(0, 0, 0) bv.Parent = hrp local elapsed = 0 local lastPos = hrp.Position local stuckCounter = 0 while isRunning and elapsed < duration do task.wait(0.05) elapsed = elapsed + 0.05 if not hrp or not hrp.Parent then break end local dir = goal - hrp.Position local remaining = dir.Magnitude if (hrp.Position - lastPos).Magnitude < 0.5 then stuckCounter = stuckCounter + 1 if stuckCounter > 10 then local humanoid = player.Character:FindFirstChild("Humanoid") if humanoid then humanoid.Jump = true end stuckCounter = 0 end else stuckCounter = 0 end lastPos = hrp.Position if remaining < 3 then break end local currentSpeed = speed * math.clamp(remaining / 10, 0.3, 1) bv.Velocity = dir.Unit * currentSpeed end bv:Destroy() if isRunning then if (hrp.Position - targetPart.Position).Magnitude < 8 then local bp = Instance.new("BodyPosition") bp.Position = targetPart.Position + Vector3.new(0, 2, 0) bp.MaxForce = Vector3.new(1e5, 1e5, 1e5) bp.P = 5000 bp.D = 500 bp.Parent = hrp task.wait(0.5) bp:Destroy() end end workspace.CurrentCamera.CameraType = Enum.CameraType.Custom task.wait(0.2) end local function startFarm() if isRunning then return end isRunning = true setSitMode(true) local uncollectedEggs = getUncollectedEggs() if #uncollectedEggs == 0 then Rayfield:Notify({ Title = "✅ Complete", Content = "All eggs collected!", Duration = 3 }) isRunning = false setSitMode(false) return end for i, part in ipairs(uncollectedEggs) do if not isRunning then break end Rayfield:Notify({ Title = "🥚 Farming", Content = string.format("Egg %d/%d", i, #uncollectedEggs), Duration = 1 }) moveToEgg(part) end setSitMode(false) isRunning = false local _, total, collected = updateStats() Rayfield:Notify({ Title = "🏁 Farm Finished", Content = string.format("%d/%d eggs collected", collected, total), Duration = 3 }) end local function stopFarm() isRunning = false setSitMode(false) Rayfield:Notify({ Title = "⏹️ Stopped", Content = "Auto farm stopped", Duration = 2 }) end local function enableNoclip() if collisionConnection then collisionConnection:Disconnect() end collisionConnection = RunService.Stepped:Connect(function() if player.Character then for _, p in ipairs(player.Character:GetDescendants()) do if p:IsA("BasePart") then p.CanCollide = false end end end end) end local function disableNoclip() if collisionConnection then collisionConnection:Disconnect() collisionConnection = nil end if player.Character then for _, p in ipairs(player.Character:GetDescendants()) do if p:IsA("BasePart") then p.CanCollide = true end end end end local function updateStats() local eggs = getAllEggs() local total = #eggs local collected = 0 for _, eggData in ipairs(eggs) do if eggData.collected then collected = collected + 1 end end return total, collected end local Window = Rayfield:CreateWindow({ Name = "Egg Auto Farm", LoadingTitle = "Egg Auto Farm", LoadingSubtitle = "By CodeBlox Studio", Theme = "AmberGlow", ConfigurationSaving = { Enabled = false } }) local FarmTab = Window:CreateTab("🥚 Egg Farm", 4483362458) FarmTab:CreateSection("📊 Statistics") local TotalEggsLabel = FarmTab:CreateButton({ Name = "Total Eggs: Loading...", Callback = function() end }) local CollectedEggsLabel = FarmTab:CreateButton({ Name = "Collected: Loading...", Callback = function() end }) FarmTab:CreateButton({ Name = "🔄 Refresh Stats", Callback = function() local total, collected = updateStats() TotalEggsLabel:Set("Total Eggs: " .. total) CollectedEggsLabel:Set("Collected: " .. collected) Rayfield:Notify({ Title = "Stats Updated", Content = string.format("%d/%d eggs", collected, total), Duration = 2 }) end }) FarmTab:CreateSection("🎮 Auto Farm Controls") local StartStopButton = FarmTab:CreateButton({ Name = "▶ START FARM", Callback = function() if isRunning then stopFarm() StartStopButton:Set("▶ START FARM") else startFarm() StartStopButton:Set("⏹ STOP FARM") end end }) FarmTab:CreateSlider({ Name = "⚡ Movement Speed", Range = {20, 200}, Increment = 5, Suffix = "studs/s", CurrentValue = playerSpeed, Callback = function(Value) playerSpeed = Value end }) FarmTab:CreateSection("⚙️ Options") local NoclipToggle = FarmTab:CreateToggle({ Name = "👻 Noclip Mode", CurrentValue = false, Callback = function(Value) noclipEnabled = Value if Value then enableNoclip() else disableNoclip() end end }) local AutoJumpToggle = FarmTab:CreateToggle({ Name = "🦘 Auto Jump (unstuck)", CurrentValue = false, Callback = function(Value) autoJump = Value end }) local SitToggle = FarmTab:CreateToggle({ Name = "💺 Sit Mode while farming", CurrentValue = true, Callback = function(Value) end }) local ManualTab = Window:CreateTab("🖱️ Manual Collect", 4483362458) ManualTab:CreateSection("📌 Click on eggs in game") ManualTab:CreateButton({ Name = "🔄 Refresh Click Detectors", Callback = function() addClickDetectorsToEggs() Rayfield:Notify({ Title = "Click Detectors", Content = "Added to all eggs!", Duration = 2 }) end }) ManualTab:CreateButton({ Name = "🚀 Teleport to nearest egg", Callback = function() local uncollected = getUncollectedEggs() if #uncollected > 0 then local nearest = nil local nearestDist = math.huge local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart") if hrp then for _, part in ipairs(uncollected) do local dist = (hrp.Position - part.Position).Magnitude if dist < nearestDist then nearestDist = dist nearest = part end end if nearest then hrp.CFrame = nearest.CFrame + Vector3.new(0, 3, 0) Rayfield:Notify({ Title = "Teleported", Content = "Moved to nearest egg", Duration = 2 }) end end else Rayfield:Notify({ Title = "No eggs", Content = "All eggs collected!", Duration = 2 }) end end }) local InfoTab = Window:CreateTab("ℹ️ Info", 4483362458) InfoTab:CreateSection("About") InfoTab:CreateButton({ Name = "CodeBlox Studio | Egg Auto Farm", Callback = function() Rayfield:Notify({ Title = "Egg Auto Farm v1.0", Content = "Auto collects eggs from all folders\nManual click collection available", Duration = 5 }) end }) task.wait(1) addClickDetectorsToEggs() local total, collected = updateStats() TotalEggsLabel:Set("Total Eggs: " .. total) CollectedEggsLabel:Set("Collected: " .. collected) Rayfield:Notify({ Title = "🥚 Egg Farm Ready", Content = string.format("%d eggs available", #getUncollectedEggs()), Duration = 3 }) task.spawn(function() while true do task.wait(10) local total, collected = updateStats() TotalEggsLabel:Set("Total Eggs: " .. total) CollectedEggsLabel:Set("Collected: " .. collected) end end)
